// Copie este arquivo para env.dart (gitignored) e preencha com as credenciais do Supabase.
// env.dart nunca deve ser commitado — só a anon key pertence ao cliente, nunca a secret key.
class Env {
  static const supabaseUrl = 'https://SEU_PROJETO.supabase.co';
  static const supabaseAnonKey = 'SUA_ANON_KEY';
}
