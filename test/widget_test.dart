import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // TODO: add widget tests once a fake Supabase client is wired for testing.
    expect(1 + 1, 2);
  });
}
